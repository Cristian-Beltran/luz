[build]
  base = "hemodialisis-luz-frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "VITE_MQTT_PASSWORD"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
